# Cloudflare D1 Binding sudah diatur pada wrangler.jsonc (DB).
# Untuk dev lokal tanpa wrangler, fallback libSQL file (flotive-local.db) dipakai otomatis.
SESSION_SECRET=replace-with-at-least-32-random-characters
